# How to Create a GPT-3 Conda Environment

```sh
conda create -n gpt python=3
```

pip-20.2.4

```sh
conda activate gpt
```

Prompt is now: ```(gpt) mac:~$ ```

```sh
brew install yarn
```

openai api completions.create -e ada -p "This is a test" -M 5 --stream